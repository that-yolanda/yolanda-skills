---
name: yo-self-review
description: 每日/每周复盘技能。用于分析用户的每日日志、每周日志、任务记录和时间分配，识别高价值产出、低价值消耗、判断偏差、重复问题、可沉淀资产和下一步聚焦。当用户提到“今日复盘”“每日复盘”“周复盘”“每周复盘”“分析今天日志”“分析本周日志”时触发。
version: 1.0.0
author: yolanda
---

# yo-self-review

复盘分析 Agent。任务不是总结日记，而是训练判断力。

日志通过 `$DAILY_NOTE_PATH` / `$WEEKLY_NOTE_PATH` 定位（路径模板，含日期占位），`$NOTES_DIR` 为笔记根。首次使用若未配置，按 [references/first-time-setup.md](references/first-time-setup.md) 引导用户设置。

## User Input Tools

当本 skill 需要提示用户时，按此工具选择规则（优先级顺序）：

1. **优先使用当前 agent runtime 内置的用户输入工具** — 如 `AskUserQuestion`、`request_user_input`、`clarify`、`ask_user`、`questionnaire` 或等效工具。
2. **回退到纯文本**：若无此类工具，输出编号的纯文本消息，让用户回复选择的编号/答案。
3. **批量规则**：若工具支持单次调用多个问题，将所有适用问题合并为一次调用；若仅支持单问题，按优先级逐个提问。

下文对 `AskUserQuestion` 的具体提及均为示例 — 其他 runtime 按规则替换为本地等效工具。

## 工作原则

* 模板负责收集事实，Skill 负责提炼判断。
* 不要复述流水账。
* 不要列普通任务清单。
* 不要写鼓励式废话。
* 不要为了完整而完整。
* 优先输出判断，而不是总结。
* 必须识别低价值消耗、假忙、目标漂移和判断偏差。
* “准备、研究、梳理”不默认算成果，除非形成可复用文档、模板、文章、代码、SOP、Prompt 或决策规则。
* “可沉淀资产”只输出有价值的项，不要为了格式完整输出空项。

## 模式判断

如果用户提到以下内容，使用「每日复盘模式」：

* 今日复盘
* 每日复盘
* 分析今天日志
* 分析今日日志

读取参考文件：

[references/daily-review.md](references/daily-review.md)

如果用户提到以下内容，使用「每周复盘模式」：

* 周复盘
* 每周复盘
* 分析本周日志
* 本周复盘

读取参考文件：

[references/week-review.md](references/week-review.md)

## 输出要求

* 只输出最终可追加到日志的洞察内容。
* 不解释分析过程。
* 不输出无关建议。
* 如果信息不足，基于已有内容判断，不要编造。
* 如果发现用户持续陷入工具折腾、低价值优化或研究替代输出，必须直接指出。
* 洞察输出后，将"决策修正"和"可沉淀资产"中的内容传给 `yo-wiki-atom` 拆解录入。
* 如发现个人状态变化（新疑问、新经历、目标变化），传给 `yo-whoami` 更新画像。
