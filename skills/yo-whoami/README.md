# yo-whoami

管理个人画像的 5 个文件（经历、当前状态、目标、偏好、疑问）。通常由其他 skill（yo-opentalk、yo-self-review、yo-wiki-review）调用，是画像的唯一写入者。

## 何时用

一般不直接用，由 yo-opentalk 等在需要更新画像时调用。

## 安装与首次配置

依赖 `WIKI_DIR`。对 agent 说「帮我配置 yo-whoami」即可引导完成（设置知识库路径）。
